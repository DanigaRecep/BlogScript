@inject IBlogService blogService

@{
    ViewData["Title"] = "Home Page";
}


@foreach (var item in blogService.GetMany(x => x.IsActive == true && x.Privacy == true))
{
    @await Component.InvokeAsync("BlogContainer", new { blog = item })
}
